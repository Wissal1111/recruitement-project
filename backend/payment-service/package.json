{
  "name": "payment-service",
  "version": "1.0.0",
  "description": "Payment service backend for recruitment survey app",
  "main": "src/server.js",
  "scripts": {
    "start": "npx prisma generate && node src/server.js",
    "dev": "nodemon src/server.js",
    "prisma:generate": "prisma generate",
    "prisma:migrate": "prisma migrate dev --name init"
  },
  "dependencies": {
    "@prisma/client": "^5.17.0",
    "body-parser": "^1.20.2",
    "cors": "^2.8.6",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "jsonwebtoken": "^9.0.3",
    "pg": "^8.11.3"
  },
  "devDependencies": {
    "nodemon": "^3.0.1",
    "prisma": "^5.17.0"
  }
}
